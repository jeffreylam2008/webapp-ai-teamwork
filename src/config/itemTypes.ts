/** Canonical name in t_items_type for monthly billing / rental fee items. */
export const MONTHLY_ITEM_TYPE_NAME = 'Monthly';

/**
 * Preferred numeric type_code when creating the Monthly row in t_items_type.
 * t_items.type stores this value as INT.
 */
export const MONTHLY_ITEM_TYPE_CODE_PREFERRED = 5;
